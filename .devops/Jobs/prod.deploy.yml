steps:
  - task: DownloadPipelineArtifact@2
    inputs:
      artifactName: "drop-prod"
      targetPath: "$(Pipeline.Workspace)/drop-prod"

  - task: ExtractFiles@1
    inputs:
      archiveFilePatterns: "$(Pipeline.Workspace)/drop-prod/build*.zip"
      destinationFolder: "$(Pipeline.Workspace)/extracted"
      cleanDestinationFolder: true

  - task: AzureWebApp@1
    displayName: "Deploy to App Service"
    inputs:
      azureSubscription: "$(azureSubscription)"
      appType: "webAppLinux"
      appName: "$(appServiceName)"
      package: "$(Pipeline.Workspace)/extracted"
      runtimeStack: "NODE|24"
      startupCommand: "npm start"
